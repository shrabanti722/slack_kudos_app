# Disable SSL certificate validation for node-gyp (if needed)
# Only use this if you're behind a corporate proxy with SSL inspection
# strict-ssl=false

# Alternative: Set certificate file if you have one
# cafile=/path/to/certificate.pem

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

